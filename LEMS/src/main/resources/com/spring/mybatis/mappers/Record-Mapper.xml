<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Record-Mapper">
	<select id="selectRecordList" resultType="record">
		select
		*
		from
		board
		<![CDATA[where recNum is not null AND rownum<=14 ]]>		  	
		order by regDate desc
	</select>
	<select id="selectRecentRecordList" resultType="record">
		<![CDATA[
		select *
		from(
			select *
			from record
			order by recDate desc, hwCode 
		)
		where rownum<=14
		]]>
	</select>
	<select id="selectRecordListByHwcode" resultType="record">
		select
		*
		from
		record
		<![CDATA[where hwCode=#{hwCode} AND rownum<=14]]>
		order by recDate desc, hwCode  
	</select>
	<select id="selectRecordListByRecDate" resultType="record">
		select *
		from record
		<![CDATA[TO_CHAR(RECDATE, 'YYYY/MM/DD HH24:MI') = #{recDate} AND rownum<=14 ]]>
		order by hwCode
	</select>
	
	<update id="insertRecordList" parameterType="record">
		insert
		into 
		record(recNum,lightState,reason,hwCode)
		values
		(#{recNum},#{lightState},#{reason},#{hwCode)
	</update>
	
</mapper>